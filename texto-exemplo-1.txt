import math;
let n = 0;
stdin(n);
let numberOfEvens = 0;

switch[n] {
	case [4] {
		math();
	}
	default {
		import math;
	}
}
while [n != 25 ] {
	math();
}
$$ isso eh um comentario
do {
	math();
	import math;
	n++;
} while [n != 25];
println(numberOfEvens);